<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Supermarket App</title>
</head>
<body style="background: linear-gradient(120deg, #b7cbe0, #94b5d1); min-height: 100vh;">

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Supermarket App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <% if (!user) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/register">Register</a>
                </li> 
                <li class="nav-item">
                    <a class="nav-link" href="/Login">Login</a>
                </li> 
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/Logout">Logout</a>
                </li> 
            <% } %>
          </ul>
        </div>
      </div>
    </nav>




<% if (user && user.role === "admin") { %>
  <div class="text-center mt-5">
    <h2>Welcome, <%= user.username %> (Admin)</h2>
  </div>

  <div class="container mt-4">
    <div class="row justify-content-center g-4">
      <!-- Manage Products -->
      <div class="col-md-5">
        <div class="card shadow-sm h-100 border-primary">
          <div class="card-body text-center">
            <h5 class="card-title text-primary">🛒 Manage Products</h5>
            <p class="card-text text-muted">You can add, edit, or remove supermarket items.</p>
            <a href="/inventory" class="btn btn-primary w-100">Go to Inventory</a>
          </div>
        </div>
      </div>

      <!-- Manage Users -->
      <div class="col-md-5">
        <div class="card shadow-sm h-100 border-success">
          <div class="card-body text-center">
            <h5 class="card-title text-success">👥 Manage Users</h5>
            <p class="card-text text-muted">You can view all users or delete users.</p>
            <a href="/viewusers" class="btn btn-success w-100">View All Users</a>
          </div>
        </div>
      </div>
    </div>

      <div class="text-center mt-5">
        <img src="/images/welcome.jpg" alt="Welcome Admin" class="img-fluid rounded shadow" style="max-width: 500px;">
        <p class="mt-3 text-muted">We're glad to have you back, Admin!</p>
      </div>

<% } else { %>
  <div class="text-center mt-5 text-white">
    <h1 style="font-family: 'Pacifico', cursive; font-size: 3rem;">
    Welcome to Supermarket App 🛍️</h1>
    <p class="lead text-light">Find your favourite items and manage your store efficiently!</p>

    <div class="mt-4">
      <a href="/login" class="btn btn-light btn-lg mx-2">Login</a>
      <a href="/register" class="btn btn-outline-light btn-lg mx-2">Register</a>
    </div>

    <div class="mt-5">
      <img src="/images/welcome2.jpg" alt="Welcome Image" class="img-fluid rounded shadow" style="max-width: 600px;">
    </div>
  </div>
<% } %>



</body>
</html>