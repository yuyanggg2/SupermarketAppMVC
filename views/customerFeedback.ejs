<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Customer Feedback</title>

  <style>
    body {
      background: linear-gradient(135deg, #ffe4ec, #ffd3e8, #ffc1e3, #ffb7de);
      min-height: 100vh;
      font-family: 'Poppins', sans-serif;
      color: #5a3854;
    }

    h2 {
      font-weight: 700;
      color: #d81b60;
      text-align: center;
      margin-bottom: 35px;
    }

    .feedback-card {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 30px;
      box-shadow: 0 10px 35px rgba(255, 0, 113, 0.25);
      padding: 40px 30px;
      max-width: 600px;
      margin: 50px auto;
      backdrop-filter: blur(6px);
    }

    label {
      font-weight: 600;
      color: #ad1457;
    }

    select, textarea {
      border-radius: 20px !important;
      box-shadow: 0 4px 12px rgba(255, 0, 113, 0.18);
      border: 1px solid #ffcfe2;
    }

    textarea:focus, select:focus {
      border-color: #ec407a;
      box-shadow: 0 0 0 4px rgba(255, 64, 129, 0.2);
    }

    .btn-pink {
      background: linear-gradient(135deg, #ff80ab, #f50057);
      border: none;
      border-radius: 30px;
      padding: 10px 30px;
      font-weight: 600;
      color: white;
      box-shadow: 0 5px 15px rgba(255, 0, 113, 0.35);
    }

    .btn-pink:hover {
      background: linear-gradient(135deg, #f50057, #ad1457);
    }

    .star-select option {
      font-size: 1.1rem;
      font-weight: 600;
    }

    .success-msg {
      text-align: center;
      font-weight: 600;
      background: rgba(197, 255, 217, 0.65);
      border-radius: 20px;
      padding: 12px 18px;
      color: #1b5e20;
      margin-bottom: 20px;
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 25px;
      color: #e91e63;
      font-weight: 600;
    }

    .back-link:hover {
      color: #ad1457;
      text-decoration: underline;
    }

  </style>
</head>

<body>

  <!-- Navbar -->
  <%- include("partials/header") %>

  <div class="container">
    
    <h2>💗 Customer Feedback 💗</h2>

    <% if (success) { %>
      <div class="success-msg">
        🌟 Thank you for your feedback! 🌟
      </div>
    <% } %>

    <div class="feedback-card">

      <form action="/submitFeedback" method="POST">

        <!-- Rating -->
        <div class="mb-4">
          <label class="form-label">Rating</label>
          <select name="rating" class="form-select star-select" required>
            <option value="">⭐ Select rating...</option>
            <% for(let i=1; i<=5; i++){ %>
              <option value="<%= i %>"><%= "⭐ ".repeat(i) %> (<%= i %>/5)</option>
            <% } %>
          </select>
        </div>

        <!-- Feedback -->
        <div class="mb-4">
          <label class="form-label">Your Feedback</label>
          <textarea name="feedback_text" class="form-control" rows="4" placeholder="Tell us something..." required></textarea>
        </div>

        <!-- Submit -->
        <div class="text-center">
          <button class="btn-pink">💌 Submit Feedback</button>
        </div>

      </form>

      <a href="/shopping" class="back-link">← Back to Shopping</a>

    </div>
  </div>

</body>
</html>
