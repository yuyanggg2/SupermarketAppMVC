<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Successful</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: #f7f8fa;
      font-family: 'Poppins', sans-serif;
      text-align: center;
      padding-top: 50px;
    }

    .success-box {
      background: white;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 6px 14px rgba(0,0,0,0.1);
      max-width: 600px;
      margin: auto;
    }

    .success-icon {
      font-size: 4rem;
      color: #4BB543;
    }

    .order-details {
      text-align: left;
      margin-top: 20px;
    }

    .btn-home {
      margin-top: 30px;
      padding: 12px 30px;
      font-size: 1.2rem;
      border-radius: 12px;
    }
  </style>

</head>
<body>

  <div class="success-box">
    <div class="success-icon">✔</div>
    <h1 class="mt-3">Order Successful!</h1>
    <p class="text-muted">Thank you for your purchase, <%= user.username %>!</p>


    <div class="order-details">
      <h4>Order Summary</h4>
      <p><strong>Address:</strong> <%= order.address %></p>
      <p><strong>Payment Method:</strong> <%= order.paymentMethod %></p>

      <h5 class="mt-4">Items:</h5>
      <ul>
        <% order.items.forEach(item => { %>
          <li><%= item.productName %> x <%= item.quantity %> — $<%= (item.price * item.quantity).toFixed(2) %></li>
        <% }) %>
      </ul>

      <h4 class="mt-3">Total: $<%= order.total.toFixed(2) %></h4>
    </div>

    <a href="/shopping" class="btn btn-primary btn-home">Return to Home</a>
  </div>

</body>
</html>
